# The Multivariate intensity estimation via tensor decomposition algorithm is a method for estimating multivariate intensity function for point processes.

# In main.py, we use a Gaussian mixture model and a sinusoidal-type intensity function as examples to demonstrate the performance of our algorithm.